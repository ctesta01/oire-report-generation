\documentclass{article}
\setlength\parindent{0pt}
\begin{document}

<<run-all, include=FALSE>>=
library(knitr)
src = NULL
for (i in 1:length(questions)) src = c(src, knit_expand('question_template.rnw'))
@

\Sexpr{paste(knit(text = src), collapse = '\n')}

\end{document}
